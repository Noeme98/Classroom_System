VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_publishable_key

# Optional: your dev machine's LAN URL so email links work on your phone (not localhost).
# Example: http://192.168.1.50:5173
# Also add the same URL under Supabase → Authentication → URL Configuration → Redirect URLs.
VITE_APP_URL=
